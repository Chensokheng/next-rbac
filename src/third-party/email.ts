export const sendWelcomeEmail = async (params: {
	to: string;
	subject: string;
	html: string;
}) => {};

export const sendResetPasswordEmail = async (params: {
	to: string;
	subject: string;
	html: string;
}) => {};
